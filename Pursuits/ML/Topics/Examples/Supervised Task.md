![[Pasted image 20240702093237.png|350]]
- Here we were trying to predict no. of Snow Cones we would sell.
- Those data points are solution or labels.(marked with arrow)
- We use both no. of Snow Cones sold and the temperature to generate the Best Fit line on the graph. 
- In the preceding graph, the data contains both a temperature and the number of snow cones sold. Both components are used to generate the linear regression shown on the graph. Our goal was to predict the number of snow cones sold, and we feed that value into the model. We are providing the model with labeled data and therefore, we are performing a _supervised machine learning task._