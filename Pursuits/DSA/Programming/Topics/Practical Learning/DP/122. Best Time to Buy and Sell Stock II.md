[122. Best Time to Buy and Sell Stock II](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/)

You are given an integer array `prices` where `prices[i]` is the price of a given stock on the `ith` day.

On each day, you may decide to buy and/or sell the stock. You can only hold **at most one** share of the stock at any time. However, you can buy it then immediately sell it on the **same day**.

Find and return _the **maximum** profit you can achieve_.

**Example 1:**

**Input:** prices = [7,1,5,3,6,4]
**Output:** 7
**Explanation:** Buy on day 2 (price = 1) and sell on day 3 (price = 5), profit = 5-1 = 4.
Then buy on day 4 (price = 3) and sell on day 5 (price = 6), profit = 6-3 = 3.
Total profit is 4 + 3 = 7.

**Example 2:**

**Input:** prices = [1,2,3,4,5]
**Output:** 4
**Explanation:** Buy on day 1 (price = 1) and sell on day 5 (price = 5), profit = 5-1 = 4.
Total profit is 4.

**Example 3:**

**Input:** prices = [7,6,4,3,1]
**Output:** 0
**Explanation:** There is no way to make a positive profit, so we never buy the stock to achieve the maximum profit of 0.

**Constraints:**

- `1 <= prices.length <= 3 * 104`
- `0 <= prices[i] <= 104`


# Using Recursion

#Unorthodox_Way
TLE  
The main issue with this approach is it's memory footprint is high which can result in MLE as well
```java
class Solution {
    private int helper(int prices[],int i,int j){
        if(j>=prices.length)return 0;
        int profit=0;
        if(prices[i] < prices[j]){
            int take = prices[j] - prices[i] + helper(prices,j,j+1);
            int notTake = helper(prices,i,j+1);
            profit = Math.max(take,notTake);
        }
        else{
            profit = helper(prices,i+1,i+2);
        }
        return profit;
    }
    public int maxProfit(int[] prices) {
        if(prices.length==1)return 0;
        return helper(prices,0,1);
    }
}
```

OR
Reducing the state space but time complexity still remain same
```java
class Solution {
    private int helper(int[] prices,int i,int canBuy){
        if(i == prices.length)return 0;
        int res=0;
        if(canBuy == 1){
            int buy = helper(prices,i+1,0) - prices[i];
            int notBuy = helper(prices,i+1,1);
            res = Math.max(buy,notBuy);
        }
        else{
            int sell = prices[i] + helper(prices,i+1,1);
            int notSell = helper(prices,i+1,0);
            res = Math.max(sell,notSell);
        }   
        return  res;
    }
    public int maxProfit(int[] prices) {
        return helper(prices,0,1);
    }
}
```

# Using Memoization

#Unorthodox_Way 
Memory Limit Exceeded (MLE)
```java
class Solution {
    int memo[][];
    private int helper(int prices[],int i,int j){
        if(j>=prices.length)return 0;
        if(memo[i][j]!=-1)return memo[i][j];
        int profit=0;
        if(prices[i] < prices[j]){
            int take = prices[j] - prices[i] + helper(prices,j,j+1);
            int notTake = helper(prices,i,j+1);
            profit = Math.max(take,notTake);
        }
        else{
            profit = helper(prices,i+1,i+2);
        }
        return memo[i][j] = profit;
    }
    public int maxProfit(int[] prices) {
        int n = prices.length;
        if(n==1)return 0;
        memo = new int[n][n];
        for(int i=0;i<n;i++){
            Arrays.fill(memo[i],-1);
        }
        return helper(prices,0,1);
    }
}
```

#Better_memoization
works
```java
class Solution {
    private int helper(int[] prices,int i,int canBuy,int[][] memo){
        if(i == prices.length)return 0;
        int res=0;
        if(memo[i][canBuy] != -1)return memo[i][canBuy];
        if(canBuy == 1){
            int buy = helper(prices,i+1,0,memo) - prices[i];
            int notBuy = helper(prices,i+1,1,memo);
            res = Math.max(buy,notBuy);
        }
        else{
            int sell = prices[i] + helper(prices,i+1,1,memo);
            int notSell = helper(prices,i+1,0,memo);
            res = Math.max(sell,notSell);
        }   
        return memo[i][canBuy] =  res;
    }
    public int maxProfit(int[] prices) {
        int n = prices.length;
        int memo[][] = new int[n][2];
        for(int i=0;i<n;i++){
            memo[i][0] = -1;
            memo[i][1] = -1;
        }
        return helper(prices,0,1,memo);
    }
}
```

### Brute DP way

```java
class Solution {
    public int maxProfit(int[] prices) {
        int n = prices.length;
        int dp[] = new int[n+1];
        for (int i = 1; i <= n; i++) {
            int cur[] = new int[n+1];
            for (int j = 0; j < i; j++) cur[j] = dp[j];
            for (int j = i; j <= n; j++) {
                cur[j] = Math.max(dp[j], dp[i-1] + (prices[j-1] - prices[i-1]));
            }
            dp = cur;
        }
        return dp[n];
    }
}
```

OR

```java
class Solution {
    public int maxProfit(int[] prices) {
        int n = prices.length;
        int dp[] = new int[n+1];
        for (int i = 1; i <= n; i++) {
            for (int j = i; j <= n; j++) {
                dp[j] = Math.max(dp[j], dp[i-1] + (prices[j-1] - prices[i-1]));
            }
        }
        return dp[n];
    }
}
```
# Using good DP approach

```java
class Solution {
    public int maxProfit(int[] prices) {
        int n = prices.length;
        int dp[][] = new int[n+1][2];
        for(int i=0;i<n;i++){
            dp[i][0] = -1;//storing profit
            dp[i][1] = -1;//storing netSale
        }
        int profit = 0;
        for(int i=n-1;i>=0;i--){
            for(int buy=0;buy<=1;buy++){
                if(buy==0){
                    profit = Math.max(dp[i+1][0],-prices[i]+dp[i+1][1]);
                }
                else{
                    profit = Math.max(dp[i+1][1],prices[i]+dp[i+1][0]);
                }
                dp[i][buy] = profit;
            }
        }
        return dp[0][0];
    }
}
```

OR

```java
class Solution {
    public int maxProfit(int[] prices) {
        int n = prices.length;
        int[][] dp = new int[n+1][2];
        for (int i = n-1; i >= 0; i--) {
            dp[i][0] = Math.max(dp[i+1][0], dp[i+1][1] - prices[i]);
            dp[i][1] = Math.max(dp[i+1][1], dp[i+1][0] + prices[i]);
        }
        return dp[0][0];
    }
}
```


OR
After Optimizing space
```java
class Solution {
    public int maxProfit(int[] prices) {
        int n = prices.length;
        int prev[] = new int[2];
        int profit = 0;
        for(int i=n-1;i>=0;i--){
            int cur[] = new int[2];
            for(int buy=0;buy<=1;buy++){
                if(buy==0){
                    profit = Math.max(prev[0],-prices[i]+prev[1]);
                }
                else{
                    profit = Math.max(prev[1],prices[i]+prev[0]);
                }
                cur[buy] = profit;
            }
            prev = cur;
        }
        return prev[0];
    }
}
```

OR

```java
class Solution {
    public int maxProfit(int[] prices) {
        int n = prices.length;
        int prevBuy = 0; //profit
        int prevSell = 0;//netSale
        for(int i=n-1;i>=0;i--){
            int curBuy = Math.max(prevBuy,prevSell-prices[i]);
            int curSell = Math.max(prevSell,prices[i]+prevBuy);
            prevBuy = curBuy;
            prevSell = curSell;
        }
        return prevBuy;
    }
}
```

OR

```java
class Solution {
    public int maxProfit(int[] prices) {
        int n = prices.length;
        int[] dp = new int[2];
        for (int i = n-1; i >= 0; i--) {
            int[] cur = new int[2];
            cur[0] = Math.max(dp[0], dp[1] - prices[i]); // actual profit
            cur[1] = Math.max(dp[1], dp[0] + prices[i]); // profit if any more stock is sold after deducting cost of that stock which comes before current
            dp = cur;
        }
        return dp[0];
    }
}
```

OR

```java
class Solution {
    public int maxProfit(int[] prices) {
        int curP = 0, nextP = 0;
        for (int i = prices.length-1; i >= 0; i--) {
            int price = prices[i];
            curP = Math.max(curP, nextP - price); //buy
            nextP = Math.max(nextP, curP + price); //sell
        }
        return curP;
    }
}
```

# Using Greedy 

```java
class Solution {
    public int maxProfit(int[] prices) {
        int buy = prices[0];
        int netProfit=0;
        for(int i=1;i<prices.length;i++){
            if(buy<prices[i]){
                netProfit += prices[i]-buy;
            }
            buy = prices[i];
        }
        return netProfit;
    }
}
```

OR

```java
class Solution {
    public int maxProfit(int[] prices) {
        int totalProfit = 0;
        for(int i = 1; i<prices.length; i++){
            if(prices[i]> prices[i-1]){
                totalProfit += prices[i] - prices[i-1];
            }
        }
        return totalProfit;
    }
}
```

OR

```java
class Solution {
    public int maxProfit(int[] prices) {
        int profit = 0;
        int buy = prices[0];
        for (int price : prices) {
            if (price - buy > 0) profit += price - buy;
            buy = price;
        }
        return profit;
    }
}
```

OR

```java
class Solution {
    public int maxProfit(int[] prices) {
        int profit = 0;
        for (int i = 1; i < prices.length; i++) {
            profit = Math.max(profit, profit + prices[i] - prices[i-1]);
        }
        return profit;
    }
}
```

AND

```java
class Solution {
    public int maxProfit(int[] prices) {
        int curP = 0, nextP = 0;
        for (int i = prices.length-1; i >= 0; i--) {
            int price = prices[i];
            curP = Math.max(curP, nextP - price); //buy
            nextP = Math.max(nextP, curP + price); //sell
        }
        return curP;
    }
}
```