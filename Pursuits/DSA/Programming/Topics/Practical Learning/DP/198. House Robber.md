[198. House Robber](https://leetcode.com/problems/house-robber/)

You are a professional robber planning to rob houses along a street. Each house has a certain amount of money stashed, the only constraint stopping you from robbing each of them is that adjacent houses have security systems connected and **it will automatically contact the police if two adjacent houses were broken into on the same night**.

Given an integer array `nums` representing the amount of money of each house, return _the maximum amount of money you can rob tonight **without alerting the police**_.

**Example 1:**

**Input:** nums = [1,2,3,1]
**Output:** 4
**Explanation:** Rob house 1 (money = 1) and then rob house 3 (money = 3).
Total amount you can rob = 1 + 3 = 4.

**Example 2:**

**Input:** nums = [2,7,9,3,1]
**Output:** 12
**Explanation:** Rob house 1 (money = 2), rob house 3 (money = 9) and rob house 5 (money = 1).
Total amount you can rob = 2 + 9 + 1 = 12.


# Using recursion

```java
class Solution {
    private int maxAmt=0;
    private void helper(int[] nums,int i,int amt){
        if(i>=nums.length){
            maxAmt= Math.max(maxAmt,amt);
            return;
        }
        helper(nums,i+2,amt+nums[i]);
        helper(nums,i+1,amt);
    }
    public int rob(int[] nums) {
        helper(nums,0,0);
        return maxAmt;
    }
}
```

OR

```java
class Solution {
    private int helper(int[] nums,int i){
        if(i>=nums.length)return 0;
        int rob = helper(nums,i+2)+nums[i];
        int skip = helper(nums,i+1);
        int maxAmt = Math.max(rob,skip);
        return maxAmt;
    }
    public int rob(int[] nums) {
        return helper(nums,0);
    }
}
```
# Using Memoization

```java
class Solution {
    private int helper(int[] nums,int i,int[] memo){
        if(i>=nums.length)return 0;
        if(memo[i]!=-1)return memo[i];
        int rob = helper(nums,i+2,memo)+nums[i];
        int skip = helper(nums,i+1,memo);
        int maxAmt = Math.max(rob,skip);
        memo[i] = maxAmt;
        return maxAmt;
    }
    public int rob(int[] nums) {
        int memo[] = new int[nums.length];
        Arrays.fill(memo,-1);
        return helper(nums,0,memo);
    }
}
```

OR

```java
class Solution {
    private int helper(int[] nums,int i,int[] memo){
        if(i<0)return 0;
        if(memo[i]!=-1)return memo[i];
        int rob = helper(nums,i-2,memo)+nums[i];
        int skip = helper(nums,i-1,memo);
        int maxAmt = Math.max(rob,skip);
        memo[i] = maxAmt;
        return maxAmt;
    }
    public int rob(int[] nums) {
        int memo[] = new int[nums.length];
        Arrays.fill(memo,-1);
        return helper(nums,nums.length-1,memo);
    }
}
```

OR

```java
class Solution {
    int[] nums;
    int n;
    int memo[];
    private int helper(int i){
        if(i>=nums.length)return 0;
        if(memo[i]!=-1)return memo[i];
        int maxi = Integer.MIN_VALUE;
        for(int j=i;j<n;j++){
            maxi = Math.max(maxi,nums[j]+helper(j+2));
        }
        return memo[i] = maxi;
    }
    public int rob(int[] nums) {
        this.nums = nums;
        n = nums.length;
        memo = new int[n+1];
        Arrays.fill(memo,-1);
        return helper(0);
    }
}
```
OR

```java
class Solution {
    private int helper(int[] nums,int[] memo,int i){
        if(i<0)return 0;
        if(memo[i]!=-1)return memo[i];
        int rob = nums[i] + helper(nums,memo,i-2);
        int skip = helper(nums,memo,i-1);
        return memo[i] = Math.max(rob,skip);
    }
    public int rob(int[] nums) {
        int memo[] = new int[nums.length];
        Arrays.fill(memo,-1);
        return helper(nums,memo,nums.length-1);
    }
}
```
# Using Tabulation

```java
class Solution {
    public int rob(int[] nums) {
        int n = nums.length;
        int dp[] = new int[n];
        dp[0] = nums[0];
        for(int i=1;i<n;i++){
            int take = nums[i];
            if(i>=2) take+=dp[i-2];
            int notTake = dp[i-1];
            dp[i] = Math.max(take,notTake);
        }
        return dp[n-1];
    }
}
```

OR

```java
class Solution {
    public int rob(int[] nums) {
        int n = nums.length;
        int dp[] = new int[n];
        dp[0] = nums[0];
        for (int i = 1; i < n; i++) {
            dp[i] = Math.max(dp[i-1], nums[i]);
            if(i>=2) dp[i] = Math.max(dp[i], dp[i-2]+nums[i]);
        }
        return dp[n-1];
    }
}
```


After space optimization
```java
class Solution {
    public int rob(int[] nums) {
        int n = nums.length;
        int prev1=nums[0];
        int prev2=0;
        for(int i=1;i<n;i++){
            int take = nums[i];
            if(i>=2) take+=prev2;
            int notTake = prev1;
            prev2=prev1;
            prev1 = Math.max(take,notTake);
        }
        return prev1;
    }
}
```

OR

```java
class Solution {
    public int rob(int[] nums) {
        int n = nums.length;
        if(n == 1) return nums[0];
        int p1 = nums[0];
        int p2 = Math.max(nums[0], nums[1]);
        int res = p2;
        for(int i = 2; i < n; i++) {
            res = Math.max(p1 + nums[i], p2);
            p1 = p2;
            p2 = res;
        }
        return res;
    }
}
```

OR
here p1 looks for the best outcome and p2 keeps the second best result and sees if second best can become best with the next house robbery as 2nd best is always at a minimum gap of 1 from the current house getting robbed
p1 may or may not be in a gap of 1 it's just at the optimal position
```java
class Solution {
    public int rob(int[] nums) {
        int n = nums.length;
        int dp[] = new int[n];
        int p1=0;
        int p2=0;
        for(int i=0;i<n;i++){
            dp[i] = p2+nums[i];
            p2=p1;
            if(p1<dp[i])p1 = dp[i];
        }
        return p1;
    }
}
```

After Optimizing space
```java
class Solution {
    public int rob(int[] nums) {
        int n = nums.length;
        int prev=0;
        int max=0;
        int cur=0;
        for(int i=0;i<n;i++){
            cur = prev+nums[i];
            prev=max;
            if(max<cur)max = cur;
        }
        return max;
    }
}
```