[778. Swim in Rising Water](https://leetcode.com/problems/swim-in-rising-water/)

You are given an `n x n` integer matrix `grid` where each value `grid[i][j]` represents the elevation at that point `(i, j)`.

It starts raining, and water gradually rises over time. At time `t`, the water level is `t`, meaning **any** cell with elevation less than equal to `t` is submerged or reachable.

You can swim from a square to another 4-directionally adjacent square if and only if the elevation of both squares individually are at most `t`. You can swim infinite distances in zero time. Of course, you must stay within the boundaries of the grid during your swim.

Return _the minimum time until you can reach the bottom right square_ `(n - 1, n - 1)` _if you start at the top left square_ `(0, 0)`.

**Example 1:**

![](https://assets.leetcode.com/uploads/2021/06/29/swim1-grid.jpg)

**Input:** grid = [[0,2],[1,3]]
**Output:** 3
Explanation:
At time 0, you are in grid location (0, 0).
You cannot go anywhere else because 4-directionally adjacent neighbors have a higher elevation than t = 0.
You cannot reach point (1, 1) until time 3.
When the depth of water is 3, we can swim anywhere inside the grid.

**Example 2:**

![](https://assets.leetcode.com/uploads/2021/06/29/swim2-grid-1.jpg)

**Input:** grid = [[0,1,2,3,4],[24,23,22,21,5],[12,13,14,15,16],[11,17,18,19,20],[10,9,8,7,6]]
**Output:** 16
**Explanation:** The final route is shown.
We need to wait until time 16 so that (0, 0) and (4, 4) are connected.

**Constraints:**

- `n == grid.length`
- `n == grid[i].length`
- `1 <= n <= 50`
- `0 <= grid[i][j] < n2`
- Each value `grid[i][j]` is **unique**.


# Using Dijkstra Algorithm

```java
class Solution {
    public int swimInWater(int[][] grid) {
        int n = grid.length;
        boolean[][] vis = new boolean[n][n];
        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[0] - b[0]);
        pq.offer(new int[]{grid[0][0], 0, 0});
        int res = 0;
        int dirs[][] = {{-1,0},{1,0},{0,-1},{0,1}};
        while(!pq.isEmpty()) {
            int[] node = pq.poll();
            int w = node[0];
            int x = node[1];
            int y = node[2];
            if(vis[x][y]) continue;
            res = Math.max(res, w);
            if (x == n-1 && y == n-1) return res;
            vis[x][y] = true;
            for (int[] dir : dirs) {
                int adjx = x + dir[0];
                int adjy = y + dir[1];
                if (isValid(adjx, adjy, n) && !vis[adjx][adjy]) {
                    pq.offer(new int[]{grid[adjx][adjy], adjx, adjy});
                }
            }
        }
        return -1;
    }
    private boolean isValid(int i, int j, int n) {
        return i>=0 && j>=0 && i<n && j<n;
    }
}
```


OR

```java
class Solution {
    public int swimInWater(int[][] grid) {
        int n = grid.length;
        boolean[][] vis = new boolean[n][n];
        Queue<int[]> pq = new PriorityQueue<>((a, b) -> a[0] - b[0]);

        pq.offer(new int[]{grid[0][0], 0, 0});
        vis[0][0] = true;
        int res = 0;
        int dirs[][] = {{-1,0},{1,0},{0,-1},{0,1}};
        while(!pq.isEmpty()) {
            int[] node = pq.poll();
            int w = node[0];
            int x = node[1];
            int y = node[2];
            res = Math.max(res, w);
            if (x == n-1 && y == n-1) return res;
            for (int[] dir : dirs) {
                int adjx = x + dir[0];
                int adjy = y + dir[1];
                if (isValid(adjx, adjy, n) && !vis[adjx][adjy]) {
                    vis[adjx][adjy] = true;
                    pq.offer(new int[]{grid[adjx][adjy], adjx, adjy});
                }
            }
        }
        return -1;
    }
    private boolean isValid(int i, int j, int n) {
        return i>=0 && j>=0 && i<n && j<n;
    }
}
```


OR
Using dist array instead of visited array as it only puts new neighbor if it's new dist is less then it minimum distance. 
Better time complexity
```java
class Solution {
    public int swimInWater(int[][] grid) {
        int n = grid.length;
        Queue<int[]> pq = new PriorityQueue<>((a, b) -> a[0] - b[0]);
        int[][] dist = new int[n][n];
        for (int i = 0; i < n; i++) {
            Arrays.fill(dist[i], n * n);
        }
        pq.offer(new int[]{grid[0][0], 0, 0});
        dist[0][0] = grid[0][0];
        int res = 0;
        int dirs[][] = {{-1,0},{1,0},{0,-1},{0,1}};
        while(!pq.isEmpty()) {
            int[] node = pq.poll();
            int w = node[0];
            int x = node[1];
            int y = node[2];
            res = Math.max(res, w);
            if (x == n-1 && y == n-1) return res;
            if (w > dist[x][y]) continue;
            for (int[] dir : dirs) {
                int adjx = x + dir[0];
                int adjy = y + dir[1];
                if (isValid(adjx, adjy, n)) {
                    int newDist = grid[adjx][adjy];
                    if (newDist < dist[adjx][adjy]) {
                        dist[adjx][adjy] = newDist;
                        pq.offer(new int[]{newDist, adjx, adjy});
                    }
                }
            }
        }
        return -1;
    }
    private boolean isValid(int i, int j, int n) {
        return i>=0 && j>=0 && i<n && j<n;
    }
}
```


OR

```java
class Solution {
    private class Pair implements Comparable<Pair>{
        int x, y, dist;
        Pair(int x, int y, int dist) {
            this.x = x;
            this.y = y;
            this.dist = dist;
        }

        @Override
        public int compareTo(Pair other) {
            return Integer.compare(this.dist, other.dist);
        }
    }
    private static int[][] dirs = {{-1, 0}, {0, -1}, {1, 0}, {0, 1}};
    public int swimInWater(int[][] grid) {
        int n = grid.length;
        if (n == 1) return grid[0][0];
        int[][] dist = new int[n][n];
        for (int i = 0; i < n; i++) {
            Arrays.fill(dist[i], (int)1e9);
        }
        PriorityQueue<Pair> pq = new PriorityQueue<>();
        pq.offer(new Pair(0, 0, grid[0][0]));
        dist[0][0] = grid[0][0];

        while (!pq.isEmpty()) {
            Pair cur = pq.poll();
            if (cur.dist > dist[cur.x][cur.y]) continue;
            for (int[] dir : dirs) {
                int x = cur.x + dir[0], y = cur.y + dir[1];
                if (x >= 0 && y >= 0 && x < n && y < n && dist[x][y] > Math.max(grid[x][y], cur.dist)) {
                    dist[x][y] = Math.max(grid[x][y], cur.dist);
                    if (x == n-1 && y == n-1) return dist[x][y];
                    pq.offer(new Pair(x, y, dist[x][y]));
                }
            }
        }
        return -1;
    }
}
```


# Using Disjoin Set

```java
class Solution {
    class DSU{
        int[] parent, rank;
        DSU(int n) {
            parent = new int[n];
            rank = new int[n];
            for (int  i = 0; i < n ;i++) {
                parent[i] = i;
            }
        }
        private int find(int node) {
            if (parent[node] == node) return node;
            return parent[node] = find(parent[node]);
        }

        public void union(int u, int v) {
            int pu = find(u);
            int pv = find(v);
            if (rank[pu] < rank[pv]) {
                parent[pu] = pv;
            } else if (rank[pv] < rank[pu]) {
                parent[pv] = pu;
            } else {
                parent[pu] = pv;
                rank[pv]++;
            }
        }
    }
    private class Pair implements Comparable<Pair>{
        int i, j, w;
        Pair(int i, int j, int w) {
            this.i = i; 
            this.j = j;
            this.w = w;
        }
        
        @Override
        public int compareTo(Pair other) {
            return Integer.compare(this.w, other.w);
        }
    }
    private static int[][] dirs = {{-1, 0}, {0, -1}, {1, 0}, {0, 1}};
    public int swimInWater(int[][] grid) {
        int n = grid.length;
        if (n == 1) return grid[0][0];
        DSU dsu = new DSU(n*n);
        Pair[] arr = new Pair[n*n];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                arr[i*n + j] = new Pair(i, j, grid[i][j]);
            }
        }
        Arrays.sort(arr);
        int res = 0;
        for (Pair cell : arr) {
            int u = cell.i * n + cell.j;
            res = Math.max(res, cell.w);
            for (int dir[] : dirs) {
                int x = cell.i + dir[0], y = cell.j + dir[1];
                if (x >= 0 && y >= 0 && x < n && y < n && grid[x][y] < cell.w) {
                    int v = x * n + y;
                    res = Math.max(res, grid[x][y]);
                    if (dsu.find(u) != dsu.find(v)) {
                        dsu.union(u, v);
                        if (dsu.find(0) == dsu.find(n*n-1)) return res;
                    }
                }
            }
        }
        return -1;
    }
}
```