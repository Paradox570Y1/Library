[1765. Map of Highest Peak](https://leetcode.com/problems/map-of-highest-peak/)

You are given an integer matrix `isWater` of size `m x n` that represents a map of **land** and **water** cells.

- If `isWater[i][j] == 0`, cell `(i, j)` is a **land** cell.
- If `isWater[i][j] == 1`, cell `(i, j)` is a **water** cell.

You must assign each cell a height in a way that follows these rules:

- The height of each cell must be non-negative.
- If the cell is a **water** cell, its height must be `0`.
- Any two adjacent cells must have an absolute height difference of **at most** `1`. A cell is adjacent to another cell if the former is directly north, east, south, or west of the latter (i.e., their sides are touching).

Find an assignment of heights such that the maximum height in the matrix is **maximized**.

Return _an integer matrix_ `height` _of size_ `m x n` _where_ `height[i][j]` _is cell_ `(i, j)`_'s height. If there are multiple solutions, return **any** of them_.

**Example 1:**

**![200](https://assets.leetcode.com/uploads/2021/01/10/screenshot-2021-01-11-at-82045-am.png)**

**Input:** isWater = [[0,1],[0,0]]
**Output:** [[1,0],[2,1]]
**Explanation:** The image shows the assigned heights of each cell.
The blue cell is the water cell, and the green cells are the land cells.

**Example 2:**

**![200](https://assets.leetcode.com/uploads/2021/01/10/screenshot-2021-01-11-at-82050-am.png)**

**Input:** isWater = [[0,0,1],[1,0,0],[0,0,0]]
**Output:** [[1,1,0],[0,1,1],[1,2,2]]
**Explanation:** A height of 2 is the maximum possible height of any assignment.
Any height assignment that has a maximum height of 2 while still meeting the rules will also be accepted.

**Constraints:**

- `m == isWater.length`
- `n == isWater[i].length`
- `1 <= m, n <= 1000`
- `isWater[i][j]` is `0` or `1`.
- There is at least **one** water cell.


# Using BFS

```java
class Solution {
    private static int[][] ops = {{0, -1}, {-1, 0}, {0, 1}, {1, 0}};
    public int[][] highestPeak(int[][] isWater) {
        int rows = isWater.length;
        int cols = isWater[0].length;
        int[][] height = new int[rows][cols];
        boolean vis[][] = new boolean[rows][cols];
        Queue<int[]> queue = new LinkedList<>();

        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                if (isWater[i][j] == 1) {
                    vis[i][j] = true;
                    queue.offer(new int[]{i, j});
                }
            }
        }

        bfs(height, isWater, vis, queue);
        return height;
    }
    
    private void bfs(int[][] height, int[][] isWater, boolean[][] vis, Queue<int[]> queue) {
        int rows = height.length;
        int cols = height[0].length;
        int h = 1;
        while(!queue.isEmpty()) {
            int size = queue.size();
            while(size-->0) {
                int[] cur = queue.poll();
                for (int[] op: ops) {
                    int x = op[0] + cur[0];
                    int y = op[1] + cur[1];
                    if (x >= 0 && y >= 0 && x < rows && y < cols && isWater[x][y] == 0 && !vis[x][y]) {
                        vis[x][y] = true;
                        height[x][y] = h;
                        queue.offer(new int[]{x, y});
                    }
                }
            }
            h++;
        }
    }
}
```

OR

```java
class Solution {
    private static int[][] ops = {{0, -1}, {-1, 0}, {0, 1}, {1, 0}};
    public int[][] highestPeak(int[][] isWater) {
        int rows = isWater.length;
        int cols = isWater[0].length;
        boolean vis[][] = new boolean[rows][cols];
        Queue<int[]> queue = new LinkedList<>();

        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                if (isWater[i][j] == 1) {
                    vis[i][j] = true;
                    isWater[i][j] = 0;
                    queue.offer(new int[]{i, j});
                }
            }
        }

        bfs(isWater, vis, queue);
        return isWater;
    }
    
    private void bfs(int[][] isWater, boolean[][] vis, Queue<int[]> queue) {
        int rows = isWater.length;
        int cols = isWater[0].length;
        int h = 1;
        while(!queue.isEmpty()) {
            int size = queue.size();
            while(size-->0) {
                int[] cur = queue.poll();
                for (int[] op: ops) {
                    int x = op[0] + cur[0];
                    int y = op[1] + cur[1];
                    if (x >= 0 && y >= 0 && x < rows && y < cols && isWater[x][y] == 0 && !vis[x][y]) {
                        vis[x][y] = true;
                        isWater[x][y] = h;
                        queue.offer(new int[]{x, y});
                    }
                }
            }
            h++;
        }
    }
}
```

OR

```java
class Solution {
    private static int[][] ops = {{0, -1}, {-1, 0}, {0, 1}, {1, 0}};
    public int[][] highestPeak(int[][] isWater) {
        int rows = isWater.length;
        int cols = isWater[0].length;
        int[][] height = new int[rows][cols];
        Queue<int[]> queue = new LinkedList<>();

        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                if (isWater[i][j] == 1) {
                    queue.offer(new int[]{i, j});
                } else {
                    height[i][j] = -1;
                }
            }
        }

        bfs(height, isWater, queue);
        return height;
    }
    
    private void bfs(int[][] height, int[][] isWater, Queue<int[]> queue) {
        int rows = height.length;
        int cols = height[0].length;
        int h = 1;
        while(!queue.isEmpty()) {
            int size = queue.size();
            while(size-->0) {
                int[] cur = queue.poll();
                for (int[] op: ops) {
                    int x = op[0] + cur[0];
                    int y = op[1] + cur[1];
                    if (x >= 0 && y >= 0 && x < rows && y < cols && height[x][y] < 0) {
                        height[x][y] = h;
                        queue.offer(new int[]{x, y});
                    }
                }
            }
            h++;
        }
    }
}
```

#### Reduced Space Complexity

```java
class Solution {
    private static int[][] ops = {{0, -1}, {-1, 0}, {0, 1}, {1, 0}};
    public int[][] highestPeak(int[][] isWater) {
        int rows = isWater.length;
        int cols = isWater[0].length;
        Queue<int[]> queue = new LinkedList<>();

        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                if (isWater[i][j] == 1) {
                    isWater[i][j] = 0;
                    queue.offer(new int[]{i, j});
                } else {
                    isWater[i][j] = -1;
                }
            }
        }

        bfs(isWater, queue);
        return isWater;
    }
    
    private void bfs(int[][] isWater,  Queue<int[]> queue) {
        int rows = isWater.length;
        int cols = isWater[0].length;
        int h = 1;
        while(!queue.isEmpty()) {
            int size = queue.size();
            while(size-->0) {
                int[] cur = queue.poll();
                for (int[] op: ops) {
                    int x = op[0] + cur[0];
                    int y = op[1] + cur[1];
                    if (x >= 0 && y >= 0 && x < rows && y < cols && isWater[x][y] == -1) {
                        isWater[x][y] = h;
                        queue.offer(new int[]{x, y});
                    }
                }
            }
            h++;
        }
    }
}
```

OR

```java
class Solution {
    private static int[][] ops = {{0, -1}, {-1, 0}, {0, 1}, {1, 0}};
    public int[][] highestPeak(int[][] isWater) {
        int rows = isWater.length;
        int cols = isWater[0].length;
        Queue<int[]> queue = new LinkedList<>();

        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                if (isWater[i][j] == 1) {
                    isWater[i][j] = -1;
                    queue.offer(new int[]{i, j});
                }
            }
        }

        bfs(isWater, queue);
        
        for (int i = 0; i < rows; i++) {
            for(int j = 0; j < cols; j++) {
                if(isWater[i][j] == -1) isWater[i][j] = 0;
            }
        }
        return isWater;
    }
    
    private void bfs(int[][] isWater,  Queue<int[]> queue) {
        int rows = isWater.length;
        int cols = isWater[0].length;
        int h = 1;
        while(!queue.isEmpty()) {
            int size = queue.size();
            while(size-->0) {
                int[] cur = queue.poll();
                for (int[] op: ops) {
                    int x = op[0] + cur[0];
                    int y = op[1] + cur[1];
                    if (x >= 0 && y >= 0 && x < rows && y < cols && isWater[x][y] == 0) {
                        isWater[x][y] = h;
                        queue.offer(new int[]{x, y});
                    }
                }
            }
            h++;
        }
    }
}
```


# Using DP

```java
class Solution {
    public int[][] highestPeak(int[][] isWater) {
        int rows = isWater.length;
        int cols = isWater[0].length;

        int[][] height = new int[rows][cols];

        for (int i = 0; i < rows; i++) {
            for (int j =0; j < cols; j++) {
                if (isWater[i][j] == 0) {
                    height[i][j] = 2001;
                }
            }
        }

        for (int i = 0; i < rows; i++) {
            for (int j = 0; j < cols; j++) {
                if (isWater[i][j] == 0) {
                    if (i > 0) {
                        height[i][j] = Math.min(height[i][j], height[i-1][j] + 1);
                    }
                    if (j > 0) {
                        height[i][j] = Math.min(height[i][j], height[i][j-1] + 1);
                    }
                }
            }
        }
        for (int i = rows-1; i >= 0; i--) {
            for (int j = cols-1; j >= 0; j--) {
                if (isWater[i][j] == 0) {
                    if (i < rows-1) {
                        height[i][j] = Math.min(height[i][j], height[i+1][j] + 1);
                    }
                    if (j < cols-1) {
                        height[i][j] = Math.min(height[i][j], height[i][j+1] + 1);
                    }
                }
            }
        }
        return height;
    }
}
```