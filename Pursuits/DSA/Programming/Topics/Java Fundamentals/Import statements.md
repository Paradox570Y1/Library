1. `import java.io.BufferedReader;`

    - Imports the `BufferedReader` class for reading text from input stream.
2. `import java.io.IOException;`
    
    - Imports the `IOException` class to handle possible I/O exceptions.
3. `import java.io.InputStreamReader;`
    
    - Imports the `InputStreamReader` class which is a bridge from byte streams to character streams.