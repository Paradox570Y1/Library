Client Side Rendering
	- 1st hop making static html call
	- 2nd hop making API call for data
	- 3rd hop processing the data and fetching the result.
Server Side Rendering
	- All 3 hops happen at once on server side.
![[Pasted image 20250814212017.png|300]]
![[Pasted image 20250814212142.png]]

- C/C++ file which `.exe` file if hosted on a server may not work on all Operating System.
- Java is platform independent and once it's executed on server then you can use the result in any OS. So these languages uses [[Server Side technology]] to run in backend.
[[Java Backend Technologies]]
- Similarly `.NET` uses `ASP` for server side technology.