The URL input type is used for entering a website URL. It provides basic validation to ensure the text entered is a valid URL (e.g., `http://example.com`).

```html
<label for="website">Website:</label>
<input type="url" id="website" name="website" placeholder="https://www.example.com" required>
```