The email input type allows the user to enter an email address. It typically provides built-in validation to ensure the entered text follows a valid email format (e.g., `user@example.com`).

```html
<label for="email">Email:</label>
<input type="email" id="email" name="email" placeholder="example@example.com" required>
```